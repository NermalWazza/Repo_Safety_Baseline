# TASKS.md

## Active

- [ ] Verify `scripts/Audit/` and `scripts/Setup/` scripts are current and tested

## Needs User Confirmation

- [ ] Add C# / .NET `.gitignore` template?
- [ ] Add Node / Vite `.gitignore` template?
- [ ] Update `detect-secrets` or `TruffleHog` versions in `.pre-commit-config.yaml`?
